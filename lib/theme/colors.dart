import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // M3 seed color
  static const Color seed = Color(0xFFD4A843);

  // Semantic accent colors
  static const Color emerald = Color(0xFF2D8B57);
  static const Color crimson = Color(0xFF8B2252);
}
