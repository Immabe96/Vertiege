# Vertiege environment configuration
# Copy to .env for local run and release builds (APK/AAB/IPA embed this file).
# CI uses GitHub secrets SUPABASE_URL and SUPABASE_ANON_KEY instead.
#
# Closed beta setup: docs/CLOSED_BETA.md
SUPABASE_URL=https://wjaphoaxalvgjnrwqjwe.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Debug-only fallback for staff review UI (production: set is_verifier in Supabase Auth)
VERIFIER_ADMIN_EMAILS=

# Do not use SUPERUSER_EMAILS in beta builds — bypasses tier gates in debug only
