# Jules Task 04: Feed 42501 RLS Fix

## Plan Approval Required
Generate a plan first and wait for explicit approval before implementation. Do not auto-approve your own plan and do not open a PR until the plan has been approved.

## Goal
Fix Supabase permission failures that prevent Nexus/world posts from loading.

## Scope
Allowed to touch:
- `supabase/migrations/**`
- `lib/services/post_service.dart`
- `lib/repositories/**`
- `lib/state/post_provider.dart`
- `test/**`
- `docs/audits/jules-full-app-audit.md`

Do not redesign UI in this task.
Do not touch unrelated feature services.

## Required Work
- Audit post/feed queries in Flutter and matching Supabase RLS policies.
- Fix policies for authenticated feed reads without exposing private/world-restricted data incorrectly.
- Verify policies for comments, reactions, bookmarks, and post media used by feed rendering.
- Add or repair indexes used by feed queries.
- If query code uses columns/policies incorrectly, fix the minimal service/repository code.
- Update audit doc with policy changes and remaining risks.

## Verification
Run:
- `flutter analyze --no-fatal-infos --no-fatal-warnings`
- `flutter test`
- Static SQL review explaining why authenticated users can read permitted posts and why denied rows remain denied.

Add or update tests for query-shape/model parsing where practical.
