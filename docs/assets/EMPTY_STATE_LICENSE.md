# Empty state illustrations (deferred)

SVGs in `assets/images/empty_states/` are from [unDraw](https://undraw.co/) via the `cuuupid/undraw-illustrations` mirror. **They are not used in the app** — too busy for the Forui minimal empty pattern.

**License:** [unDraw license](https://undraw.co/license) — free for commercial use; no attribution required.

**Production UI:** icon-only `AppEmptyState` in `lib/widgets/core/empty_state.dart` (muted circle + `FButton` CTA).

ChatGPT `assets/generated/empty-*.jpg` files remain deprecated.
